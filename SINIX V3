-- ─── SINIX UPDATE NOTIFICATION ───────────────────────────────────────────────
-- Colle ce bloc EN HAUT de ton script principal, avant tout le reste.
-- Modifie les 3 variables ci-dessous à chaque update.

local UPDATE_DATE    = "21 Juillet 2025"
local UPDATE_MESSAGE = "Ajout D'anticheat Pour proteger la comu !"
local DISCORD_LINK   = "discord.gg/WBA3pVEwMR"

-- ─────────────────────────────────────────────────────────────────────────────

local TweenService  = game:GetService("TweenService")
local RunService    = game:GetService("RunService")
local GuiService    = game:GetService("GuiService")
local Players       = game:GetService("Players")
local player        = Players.LocalPlayer

-- ─── RAINBOW ─────────────────────────────────────────────────────────────────

local _hue = 0
local _rainbowObjs = {}

local function _getRainbow()
    _hue = (_hue + 1) % 360
    return Color3.fromHSV(_hue / 360, 0.85, 1)
end
local function _addRainbow(obj, prop)
    table.insert(_rainbowObjs, { obj = obj, prop = prop })
end

RunService.Heartbeat:Connect(function()
    local c = _getRainbow()
    for i = #_rainbowObjs, 1, -1 do
        local e = _rainbowObjs[i]
        if e.obj and e.obj.Parent then e.obj[e.prop] = c
        else table.remove(_rainbowObjs, i) end
    end
end)

-- ─── HELPERS ─────────────────────────────────────────────────────────────────

local function _corner(p, r)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, r or 8)
    c.Parent = p
    return c
end

local function _stroke(p, t, col)
    local s = Instance.new("UIStroke")
    s.Thickness = t or 1.5
    if col then s.Color = col end
    s.Parent = p
    return s
end

local function _lbl(p, props)
    local l = Instance.new("TextLabel")
    l.BackgroundTransparency = 1
    l.BorderSizePixel = 0
    for k, v in pairs(props) do l[k] = v end
    l.Parent = p
    return l
end

-- ─── SCREENGUI ───────────────────────────────────────────────────────────────

local gui = Instance.new("ScreenGui")
gui.Name           = "SinixUpdateNotif"
gui.ResetOnSpawn   = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent         = player:WaitForChild("PlayerGui")

-- ─── FOND ASSOMBRI ───────────────────────────────────────────────────────────

local overlay = Instance.new("Frame")
overlay.Size                   = UDim2.new(1, 0, 1, 0)
overlay.BackgroundColor3       = Color3.fromRGB(0, 0, 0)
overlay.BackgroundTransparency = 1
overlay.BorderSizePixel        = 0
overlay.ZIndex                 = 90
overlay.Parent                 = gui

TweenService:Create(overlay,
    TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
    { BackgroundTransparency = 0.55 }):Play()

-- ─── CARTE PRINCIPALE ────────────────────────────────────────────────────────

local W, H = 440, 300

local card = Instance.new("Frame")
card.Size                   = UDim2.new(0, 0, 0, 0)
card.Position               = UDim2.new(0.5, 0, 0.5, 0)
card.AnchorPoint            = Vector2.new(0.5, 0.5)
card.BackgroundColor3       = Color3.fromRGB(14, 13, 22)
card.BorderSizePixel        = 0
card.ZIndex                 = 92
card.BackgroundTransparency = 1
card.Parent                 = gui
_corner(card, 18)

local cardGrad = Instance.new("UIGradient")
cardGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0,   Color3.fromRGB(32, 26, 58)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(18, 16, 30)),
    ColorSequenceKeypoint.new(1,   Color3.fromRGB(10, 10, 16)),
})
cardGrad.Rotation = 135
cardGrad.Parent   = card

_addRainbow(_stroke(card, 2), "Color")

-- barre top rainbow
local topBar = Instance.new("Frame")
topBar.Size            = UDim2.new(0.5, 0, 0, 3)
topBar.Position        = UDim2.new(0.25, 0, 0, 0)
topBar.BorderSizePixel = 0
topBar.ZIndex          = 93
topBar.Parent          = card
_addRainbow(topBar, "BackgroundColor3")
_corner(topBar, 2)

-- ─── CROIX ───────────────────────────────────────────────────────────────────

local closeBtn = Instance.new("TextButton")
closeBtn.Size             = UDim2.new(0, 26, 0, 26)
closeBtn.Position         = UDim2.new(1, -34, 0, 8)
closeBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
closeBtn.BorderSizePixel  = 0
closeBtn.Font             = Enum.Font.GothamBold
closeBtn.TextSize         = 16
closeBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
closeBtn.Text             = "×"
closeBtn.AutoButtonColor  = false
closeBtn.ZIndex           = 98
closeBtn.ClipsDescendants = true
closeBtn.Parent           = card
_corner(closeBtn, 6)

closeBtn.MouseEnter:Connect(function()
    TweenService:Create(closeBtn, TweenInfo.new(0.12),
        { BackgroundColor3 = Color3.fromRGB(220, 70, 70) }):Play()
end)
closeBtn.MouseLeave:Connect(function()
    TweenService:Create(closeBtn, TweenInfo.new(0.12),
        { BackgroundColor3 = Color3.fromRGB(180, 50, 50) }):Play()
end)
closeBtn.MouseButton1Click:Connect(function()
    local out = TweenService:Create(card,
        TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
        { Size = UDim2.new(0, W - 60, 0, H - 40), BackgroundTransparency = 1 })
    TweenService:Create(overlay,
        TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In),
        { BackgroundTransparency = 1 }):Play()
    out:Play()
    out.Completed:Connect(function() gui:Destroy() end)
end)

-- ─── ICÔNE ───────────────────────────────────────────────────────────────────

local iconBg = Instance.new("Frame")
iconBg.Size             = UDim2.new(0, 46, 0, 46)
iconBg.Position         = UDim2.new(0.5, -23, 0, 16)
iconBg.BackgroundColor3 = Color3.fromRGB(26, 22, 48)
iconBg.BorderSizePixel  = 0
iconBg.ZIndex           = 93
iconBg.Parent           = card
_corner(iconBg, 12)
_addRainbow(_stroke(iconBg, 1.5), "Color")
_lbl(iconBg, { Size = UDim2.new(1,0,1,0), Font = Enum.Font.GothamBold, TextSize = 24, Text = "🔄", ZIndex = 94 })

-- ─── TITRE ───────────────────────────────────────────────────────────────────

local titleLbl = _lbl(card, {
    Size           = UDim2.new(1, -20, 0, 28),
    Position       = UDim2.new(0, 10, 0, 72),
    Font           = Enum.Font.GothamBold,
    TextSize       = 20,
    TextColor3     = Color3.fromRGB(255, 255, 255),
    TextXAlignment = Enum.TextXAlignment.Center,
    Text           = "Updating SINIX...",
    ZIndex         = 93,
})
_addRainbow(titleLbl, "TextColor3")

-- ─── SÉPARATEUR 1 ────────────────────────────────────────────────────────────

local sep1 = Instance.new("Frame")
sep1.Size             = UDim2.new(0.85, 0, 0, 1)
sep1.Position         = UDim2.new(0.075, 0, 0, 110)
sep1.BackgroundColor3 = Color3.fromRGB(55, 50, 90)
sep1.BorderSizePixel  = 0
sep1.ZIndex           = 93
sep1.Parent           = card

-- ─── BOÎTE MESSAGE ───────────────────────────────────────────────────────────

local msgBox = Instance.new("Frame")
msgBox.Size             = UDim2.new(0.86, 0, 0, 88)
msgBox.Position         = UDim2.new(0.07, 0, 0, 120)
msgBox.BackgroundColor3 = Color3.fromRGB(22, 20, 38)
msgBox.BorderSizePixel  = 0
msgBox.ZIndex           = 93
msgBox.Parent           = card
_corner(msgBox, 10)
_addRainbow(_stroke(msgBox, 1.2), "Color")

_lbl(msgBox, {
    Size           = UDim2.new(1, -14, 0, 18),
    Position       = UDim2.new(0, 10, 0, 6),
    Font           = Enum.Font.GothamBold,
    TextSize       = 10,
    TextColor3     = Color3.fromRGB(130, 100, 255),
    TextXAlignment = Enum.TextXAlignment.Left,
    Text           = "📝  NOTES DE MISE À JOUR",
    ZIndex         = 94,
})

local msgSep = Instance.new("Frame")
msgSep.Size             = UDim2.new(0.9, 0, 0, 1)
msgSep.Position         = UDim2.new(0.05, 0, 0, 26)
msgSep.BackgroundColor3 = Color3.fromRGB(40, 36, 68)
msgSep.BorderSizePixel  = 0
msgSep.ZIndex           = 94
msgSep.Parent           = msgBox

_lbl(msgBox, {
    Size           = UDim2.new(1, -14, 0, 56),
    Position       = UDim2.new(0, 10, 0, 30),
    Font           = Enum.Font.Gotham,
    TextSize       = 12,
    TextColor3     = Color3.fromRGB(210, 205, 240),
    TextXAlignment = Enum.TextXAlignment.Left,
    TextYAlignment = Enum.TextYAlignment.Top,
    TextWrapped    = true,
    Text           = UPDATE_MESSAGE,
    ZIndex         = 94,
})

-- ─── SÉPARATEUR 2 ────────────────────────────────────────────────────────────

local sep2 = Instance.new("Frame")
sep2.Size             = UDim2.new(0.85, 0, 0, 1)
sep2.Position         = UDim2.new(0.075, 0, 0, 218)
sep2.BackgroundColor3 = Color3.fromRGB(55, 50, 90)
sep2.BorderSizePixel  = 0
sep2.ZIndex           = 93
sep2.Parent           = card

-- ─── DATE ────────────────────────────────────────────────────────────────────

_lbl(card, {
    Size           = UDim2.new(1, -20, 0, 18),
    Position       = UDim2.new(0, 10, 0, 228),
    Font           = Enum.Font.GothamSemibold,
    TextSize       = 12,
    TextColor3     = Color3.fromRGB(180, 170, 220),
    TextXAlignment = Enum.TextXAlignment.Center,
    Text           = "📅  Mise à jour du " .. UPDATE_DATE,
    ZIndex         = 93,
})

-- ─── BOUTON DISCORD (copie dans le presse-papier) ────────────────────────────

local discBtn = Instance.new("TextButton")
discBtn.Size             = UDim2.new(0.82, 0, 0, 30)
discBtn.Position         = UDim2.new(0.09, 0, 0, 254)
discBtn.BackgroundColor3 = Color3.fromRGB(55, 64, 180)
discBtn.BorderSizePixel  = 0
discBtn.Font             = Enum.Font.GothamBold
discBtn.TextSize         = 12
discBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
discBtn.Text             = "💬  " .. DISCORD_LINK .. "   —   clique pour copier"
discBtn.AutoButtonColor  = false
discBtn.ZIndex           = 93
discBtn.ClipsDescendants = true
discBtn.Parent           = card
_corner(discBtn, 8)

discBtn.MouseEnter:Connect(function()
    TweenService:Create(discBtn, TweenInfo.new(0.12),
        { BackgroundColor3 = Color3.fromRGB(80, 95, 220) }):Play()
end)
discBtn.MouseLeave:Connect(function()
    TweenService:Create(discBtn, TweenInfo.new(0.12),
        { BackgroundColor3 = Color3.fromRGB(55, 64, 180) }):Play()
end)
discBtn.MouseButton1Click:Connect(function()
    pcall(function()
        setclipboard("https://discord.gg/WBA3pVEwMR")
    end)
    discBtn.Text = "✅  Lien copié !"
    TweenService:Create(discBtn, TweenInfo.new(0.12),
        { BackgroundColor3 = Color3.fromRGB(50, 160, 90) }):Play()
    task.wait(2)
    if discBtn and discBtn.Parent then
        discBtn.Text = "💬  " .. DISCORD_LINK .. "   —   clique pour copier"
        TweenService:Create(discBtn, TweenInfo.new(0.12),
            { BackgroundColor3 = Color3.fromRGB(55, 64, 180) }):Play()
    end
end)

-- ─── BARRE DE PROGRESSION ────────────────────────────────────────────────────

local trackBg = Instance.new("Frame")
trackBg.Size             = UDim2.new(0.82, 0, 0, 5)
trackBg.Position         = UDim2.new(0.09, 0, 0, 288)
trackBg.BackgroundColor3 = Color3.fromRGB(30, 28, 50)
trackBg.BorderSizePixel  = 0
trackBg.ZIndex           = 93
trackBg.Parent           = card
_corner(trackBg, 3)

local fill = Instance.new("Frame")
fill.Size             = UDim2.new(0, 0, 1, 0)
fill.BackgroundColor3 = Color3.fromRGB(130, 100, 255)
fill.BorderSizePixel  = 0
fill.ZIndex           = 94
fill.Parent           = trackBg
_corner(fill, 3)
_addRainbow(fill, "BackgroundColor3")

-- ─── ANIMATION ENTRÉE ────────────────────────────────────────────────────────

task.spawn(function()
    task.wait(0.1)
    card.BackgroundTransparency = 1
    TweenService:Create(card,
        TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        { Size = UDim2.new(0, W, 0, H) }):Play()
    TweenService:Create(card,
        TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        { BackgroundTransparency = 0 }):Play()
    task.wait(0.35)
    TweenService:Create(fill,
        TweenInfo.new(1.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        { Size = UDim2.new(1, 0, 1, 0) }):Play()
end)

-- ─── FIN DU BLOC UPDATE — ton script principal continue en dessous ─────────
